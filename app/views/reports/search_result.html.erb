<table>
  <tr>
    <th>Order ID</th>
    <th>Date</th>
    <th>Name</th>
    <th>Email</th>
  </tr>
<% @orders_placed.each do |order|%>
  <tr>
    <td><%= link_to "#{order[0]}" , report_path( :id => "#{order[0]}") , :method => :get %></td>
    <td><%= order[1]%></td>
    <td><%= order[2]%></td>
    <td><%= order[3]%></td>
  </tr>
  <tr>
<% end %>
</table>